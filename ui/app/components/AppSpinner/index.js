import { Spin } from 'antd';
import styled from 'styled-components';

const AppSpinner = styled(Spin)`
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
`;

export default AppSpinner;
