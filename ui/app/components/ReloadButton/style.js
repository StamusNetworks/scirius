import styled from 'styled-components';

export const ReloadButton = styled.div`
  display: flex;
  align-items: center;

  background: transparent;
  border: none;

  padding: 0 20px;
  gap: 8px;

  svg {
    width: 24px;
    height: 24px;
  }
`;
