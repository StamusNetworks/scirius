import { HUNT_URL } from 'ui/config';

const EnrichedHunting = () => null;

EnrichedHunting.metadata = {
  category: 'STAMUS_ND',
  url: () => `${window.location.origin}${HUNT_URL}`,
  title: 'Enriched Hunting',
}

export default EnrichedHunting;
